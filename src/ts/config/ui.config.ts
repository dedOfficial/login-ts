const UI = {
  form: <HTMLFormElement>document.forms['loginForm'],
  inputEmail: <HTMLInputElement>document.getElementById('email'),
  inputPassword: <HTMLInputElement>document.getElementById('password'),
};

export default UI;
